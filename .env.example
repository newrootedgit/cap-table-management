# Database (PostgreSQL - use Vercel Postgres, Neon, or Supabase)
DATABASE_URL="postgresql://user:password@host:5432/captable?sslmode=require"

# Basic Auth (protects the entire app)
BASIC_AUTH_USER="admin"
BASIC_AUTH_PASSWORD="captable2024"
